language: java

jdk:
  - openjdk11

before_install:
 - chmod +x gradlew

jobs:
  include:
    - os: linux
      script:
        - pwd
        - ls -la
        - chmod +x runtest.sh
        - ./runtest.sh
    - os: osx
      script:
        - pwd
        - ls -la
        - chmod +x runtest.sh
        - ./runtest.sh

notifications:
  email: false
