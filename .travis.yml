language: java

jdk:
  - openjdk11

before_install:
 - chmod +x gradlew

jobs:
  include:
    - os: linux
      script:
        - cd text-ui-test
        - chmod +x runtest.sh
        - ./runtest.sh
    - os: osx
      script:
        - cd text-ui-test
        - chmod +x runtest.sh
        - ./runtest.sh

notifications:
  email: false
