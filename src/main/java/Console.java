public class Console {
    /**
     * Console logs text passed into the parameter.
     *
     * @param text text to be logged
     */
    public static void log(String text) {
        System.out.println(text);
    }
}
