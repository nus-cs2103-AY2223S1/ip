package duke.command;

import java.util.function.Function;

/**
 * A wrapper class that wraps a command's string instruction and the command itself.
 *
 * @param <T> Any subtype of Command.
 */

public abstract class CommandSupplier<T extends Command> implements Function<String, T> {

    T 

    public String getInstruction() {
        return
    }
}
