package duke.main;

/**
 * CommandManager lists down all possible commands that can be taken by user
 */
public enum CommandManager {
    bye,
    list,
    mark,
    unmark,
    delete,
    todo,
    deadline,
    event,
    find,
}
