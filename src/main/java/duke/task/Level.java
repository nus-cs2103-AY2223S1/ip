package duke.task;

/**
 * Defines the {@code Level} type.
 * {@code Level} is an {@code enum} type which can
 * take on values "LOW", "MEDIUM" or "HIGH".
 */
public enum Level {
    LOW, MEDIUM, HIGH
}
