package duke;

/**
 * Main class that calls the parsing of the user input.
 */
public class Duke {
    public static void main(String[] args) {
        Parser p = new Parser();
        p.startParse();
    }
}
