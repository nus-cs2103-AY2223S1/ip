package duke;

import javafx.application.Application;

/**
 * Alternative entry into the duke chatbot.
 */
public class Launcher {
    /**
     * Runs the chatbot.
     *
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}