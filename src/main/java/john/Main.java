package john;

import javafx.application.Application;

/**
 * The entry point of the program which launches the GUI.
 */
public class Main {
    /**
     * Launches the JavaFX program by calling the start method in John.java.
     * @param args The initialised arguments.
     */
    public static void main(String[] args) {
        Application.launch(John.class, args);
    }
}
