package duke;

import java.time.LocalDate;

/**
 * Substitute the Date with a general implementation.
 */
public class DateStub extends Date {
    public DateStub(LocalDate date) {
        super(date);
    }
}
