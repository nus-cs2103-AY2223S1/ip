package duke;

import java.util.List;

/**
 * Substitute the TaskList with a general implementation.
 */
public class TaskListStub extends TaskList {
    public TaskListStub(List<Task> taskList) {
        super(taskList);
    }
}
