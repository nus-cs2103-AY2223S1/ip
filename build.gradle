plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '5.1.0'

    //@@author cheeheng-reused
    // Reused from https://se-education.org/guides/tutorials/checkstyle.html
    id 'checkstyle'
    //@@author

    //@@author cheeheng-reused
    // Reused from https://openjfx.io/openjfx-docs/#gradle
    id 'org.openjfx.javafxplugin' version '0.0.10'
    //@@author
}

repositories {
    mavenCentral()
}

// @@author cheeheng-reused
// Reused from https://openjfx.io/openjfx-docs/#gradle
javafx {
    version = "11.0.2"
    modules = [ 'javafx.controls', 'javafx.fxml' ]
}
//@@author

dependencies {
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.5.0'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.5.0'
}

test {
    useJUnitPlatform()

    testLogging {
        events "passed", "skipped", "failed"

        showExceptions true
        exceptionFormat "full"
        showCauses true
        showStackTraces true
        showStandardStreams = false
    }
}

application {
    mainClassName = "jude.Launcher"
}

shadowJar {
    archiveBaseName = "jude"
    archiveClassifier = null
    archiveVersion = "0.3"
}

run {
    standardInput = System.in

    // @@author cheeheng-reused
    // Reused from https://se-education.org/guides/tutorials/gradle.html#creating-a-jar-file
    enableAssertions = true
    //@@author
}

// @@author cheeheng-reused
// Reused from https://se-education.org/guides/tutorials/checkstyle.html with minor modifications
checkstyle {
    toolVersion = '10.3.2'
}
//@@author