package duke;

/**
 * An interface that describes objects that accept an input.
 */
public interface InputAcceptor {
    /**
     * Accepts an input.
     * @param input The input to be accepted.
     */
    void input(String input);
}
